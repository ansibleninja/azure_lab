---
- hosts: localhost
  connection: local
  tasks:

    - name: Create a resource
      azure_rm_resourcegroup:
        name: table6fyansibletest
        location: koreacentral
        tags:
          testing: testing
          delete: never
