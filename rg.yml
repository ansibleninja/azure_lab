---
- hosts: localhost
  connection: local
  tasks:

    - name: Create a resource
      azure_rm_resourcegroup:
        name: {{ rg_name }}
        location: koreacentral
        tags:
          testing: testing
          delete: never
